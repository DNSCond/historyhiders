{
  "private": true,
  "name": "historyhiders",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types ./tools/build.ts --minify",
    "deploy": "npm run build",
    "dev": "devvit playtest",
    "login": "devvit login",
    "launch": "npm run protobuf-generate && npm run deploy && devvit publish",
    "type-check": "tsc --build",
    "protobuf-generate": "npx protoc --ts_proto_out=. --ts_proto_opt=esModuleInterop=true,outputServices=none src/server/userdata-protobuf.proto"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "dependencies": {
    "@devvit/web": "0.12.19",
    "anthelpers": "^0.2.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "22.12.0",
    "esbuild": "0.27.2",
    "ts-proto": "^2.11.6",
    "typescript": "5.9.3"
  }
}
